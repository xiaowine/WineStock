[package]
name = "winestock-shared"
version.workspace = true
edition.workspace = true
publish = false

[lib]
# shared 只放平台无关配置和基础辅助类型，不能反向依赖 core。
name = "winestock_shared"
path = "src/lib.rs"
crate-type = ["lib"]

[dependencies]
garde = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
